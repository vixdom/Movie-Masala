# Environment Configuration for Dynamic Port System

# =============================================================================
# PORT CONFIGURATION
# =============================================================================

# Main server port (will use dynamic allocation if not specified or unavailable)
# Default: Dynamically allocated starting from 5000
PORT=5000

# Vite HMR (Hot Module Replacement) port for development
# Default: Dynamically allocated starting from 24678
VITE_HMR_PORT=24678

# API proxy target for development (where API server runs)
# Default: http://localhost:3000 (dynamically allocated)
API_TARGET=http://localhost:3000

# =============================================================================
# APPLICATION CONFIGURATION  
# =============================================================================

# Application environment
NODE_ENV=development

# Enable request/response logging
ENABLE_LOGGING=true

# CORS origins (comma-separated for multiple origins)
CORS_ORIGIN=*

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# Database connection string (required in production)
# DATABASE_URL=postgresql://username:password@localhost:5432/database

# =============================================================================
# DYNAMIC PORT SYSTEM NOTES
# =============================================================================

# This application uses dynamic port allocation to prevent conflicts:
#
# 1. If environment variables specify ports, they are tried first
# 2. If specified ports are unavailable, the system automatically finds alternatives
# 3. Port allocation is logged for debugging and monitoring
# 4. No hardcoded ports exist in the codebase to prevent future conflicts
#
# Port Ranges Used:
# - Web Server: 5000-5100 (development), 5000-5010 (production)
# - API Server: 3000-3100 (development), 3000-3010 (production)  
# - Vite HMR: 24678-24778 (development only)
#
# To override default behavior:
# - Set specific port numbers in environment variables above
# - Ports will be validated and fallbacks used if unavailable
# - Check server logs for actual allocated ports